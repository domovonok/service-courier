global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - '/etc/prometheus/alerts.yml'

scrape_configs:
  - job_name: 'courier-metrics'
    static_configs:
      - targets: ['service-courier:8080']
        labels:
          service: 'courier-metrics'